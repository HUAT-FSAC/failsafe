<launch>

  <arg name="keep_running" default="false"/>

  <node pkg="failsafe" type="failsafe_node" name="failsafe">
    <param name="keep_running" value="$(arg keep_running)"/> 
    <rosparam command="load" file="$(find failsafe)/config/config.yaml" />
  </node>

</launch>