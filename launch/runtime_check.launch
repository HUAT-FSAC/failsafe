<launch>

  <arg name="keep_running" default="true"/>

  <node pkg="failsafe" type="failsafe_node" name="failsafe">
    <param name="keep_running" value="$(arg keep_running)"/>
    <rosparam command="load" file="$(find my_pkg)/config/config.yaml" />
  </node>

</launch>